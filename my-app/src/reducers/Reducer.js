// import { STUFF FROM ACTIONS } from "../actions";

    const initialState = {

    };

    export const Reducer = (state = initialState, action) => {
        switch(action.type) {
            default:
                return state
        }
    }